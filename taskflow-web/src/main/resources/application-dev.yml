server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskflow?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379

taskflow:
  upload:
    path: ./uploads/

logging:
  level:
    com.taskflow: debug